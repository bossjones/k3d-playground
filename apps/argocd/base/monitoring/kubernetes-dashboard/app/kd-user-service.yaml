apiVersion: v1
kind: ServiceAccount
metadata:
  name: kd-user
  annotations:
    kubernetes.io/service-account.name: "kd-user"
  # namespace: kubernetes-dashboard
  namespace: monitoring
# type: kubernetes.io/service-account-token
